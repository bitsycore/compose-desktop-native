package androidx.compose.ui.platform

// ==================
// MARK: PlatformTextInputMethodRequest — native actual (empty marker)
// ==================

/**
 * Native actual for the `expect interface`. Empty marker — no platform
 * text-input session yet on the desktop path.
 */
actual interface PlatformTextInputMethodRequest
