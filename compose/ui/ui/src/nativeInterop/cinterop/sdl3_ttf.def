headers = SDL3_ttf/SDL_ttf.h
headerFilter = SDL3_ttf/**
depends = sdl3

# See sdl3.def — paths come from :ui build.gradle.kts (libs/SDL3_ttf/include)
# and the static libSDL3_ttf.a is linked into the app executable.
