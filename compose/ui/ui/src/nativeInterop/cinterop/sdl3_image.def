headers = SDL3_image/SDL_image.h
headerFilter = SDL3_image/**
depends = sdl3

# See sdl3.def — paths come from :ui build.gradle.kts (libs/SDL3_image/include)
# and the static libSDL3_image.a + vendored codec archives (libpng16, libz,
# libwebp*, libsharpyuv) are linked into the app executable.
