headers = ft2build.h freetype/freetype.h freetype/ftmm.h freetype/ftoutln.h freetype/ftbitmap.h
headerFilter = freetype2/** ft2build.h freetype/**

# See sdl3.def — paths come from :ui build.gradle.kts (libs/FreeType/include/freetype2)
# and the static libfreetype.a is linked into the app executable.
